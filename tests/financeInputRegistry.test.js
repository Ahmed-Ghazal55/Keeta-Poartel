const a=require("assert"),R=require("../src/finance/financeInputRegistry");a.equal(R.list().length,14);a.equal(R.get("company_invoice").requiredForFutureSettlement,true);a.equal(R.get("vat_zatca_placeholder").futurePromptOwner,"future");const routes=R.importRoutes();a.equal(Object.keys(routes).length,9);a.equal(routes.company_invoice_import.saveImplemented,false);a(Object.values(routes).every(x=>x.readOnlyEntry&&x.nonAuditing));console.log("financeInputRegistry: 6/6 passed");
