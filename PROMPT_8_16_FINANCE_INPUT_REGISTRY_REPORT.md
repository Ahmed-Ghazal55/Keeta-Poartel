# Prompt 8.16 Finance Input Registry Report

Date: 2026-08-04

The finance registry defines 14 expected inputs with future-settlement/final-close requirements, source modules, allowed statuses, template IDs, read-only Import routes, validation rules, weights, and future owners. Nine finance import routes explicitly set `readOnlyEntry`, `nonAuditing`, and `saveImplemented: false`.
